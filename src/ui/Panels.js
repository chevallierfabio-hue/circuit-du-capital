// src/ui/Panels.js — facade M0. Quest, leviers, journal, circuit bar :
// pour l'instant gérés directement par app.js sur les éléments DOM extraits
// du HTML papier (cf. index.html). M1+ extraira chaque panneau.
export const STATUS = 'facade M0 — panneaux gérés par src/app.js';
