// src/ui/HUD.js — facade M0. HUD = bandeau Capital/Dette/Profit/Stocks/
// Chômage/Colère/Risque + objectifs ; les fonctions de rendu (updateHUD,
// updateMarx, renderLeviers, renderCircuitBar, renderQuest) sont définies
// dans src/app.js et exposées ici pour le découpage annoncé.
export {
  updateHUD, updateMarx, renderLeviers, renderCircuitBar, renderQuest,
} from '../app.js';
